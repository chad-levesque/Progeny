#include "DemoTypes.h"

SPHERECOORDS::SPHERECOORDS() : _radius(0.0f), _inclination(0.0f), _azimuth(0.0f)
{
	
}

SPHERECOORDS::SPHERECOORDS(float radius, float inclination, float azimuth) : _radius(radius), _inclination(inclination), _azimuth(azimuth)
{

}
